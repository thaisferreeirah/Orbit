<Page x:Class="RedeSocial.PageTesteComunidade"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RedeSocial"
      mc:Ignorable="d" 
      d:DesignHeight="670" d:DesignWidth="1150" 
      Title="PageTesteComunidade">


    <Page.Resources>
        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="{StaticResource ArimoFont}"/>
            <Setter Property="Background" Value="#E0E4E8"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border CornerRadius="15"  Background="#E0E4E8">
                                <ScrollViewer x:Name="PART_ContentHost" Margin="0" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="1" Width="340"  CornerRadius="15" Margin="10,10,10,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0"  Background="#FFFFFFFF" CornerRadius="15">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="3" ShadowDepth="0"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBox x:Name="caixaPesquisa" Grid.Row="0" VerticalContentAlignment="Center" Padding="14,0,14,0" Height="35" Style="{StaticResource SearchBox}" Margin="10,37,10,15"/>
                        <TextBlock Grid.Row="0" Text="Pesquisar comunidades" FontSize="14" FontFamily="{StaticResource ArimoFont}"  Foreground="#9094B6" IsHitTestVisible="False" Visibility="{Binding ElementName=caixaPesquisa, Path=Text.IsEmpty, Converter={StaticResource BoolToVis}}" Margin="26,55,20,34"/>
                        <Label Grid.Row="1" x:Name="AbrirTodasAsComunidades" Content="Todas as Comunidades" Style="{StaticResource estiloLabelLogin}" Margin="85,10,85,15" HorizontalContentAlignment="Center" MouseLeftButtonDown="AbrirTodasAsComunidades_Click"/>
                        <Button Grid.Row="2" Content="Criar uma nova comunidade" Height="35" Style="{StaticResource EstiloBotaoCinza}" Margin="50,10,50,36" Click="AbrirCriarComunidade_Click"/>
                        
                    </Grid>
                </Border>
                <Rectangle Grid.Row="1" Stroke="#DADCDF" Margin="20,15,20,15"/>
                <Label Grid.Row="2" Content="Minhas Comunidades" FontSize="16" FontFamily="{StaticResource ArimoFont}" FontWeight="Bold"  Padding="15,0,0,0"/>
                <Grid x:Name="minhasComunidades" Grid.Row="3"/>
            </Grid>
        </Border>
        <Frame x:Name="frameComunidade"  Width="778" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Column="2" Margin="0,10,0,0"/>
    </Grid>
</Page>





